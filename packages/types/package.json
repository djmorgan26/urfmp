{
  "name": "@urfmp/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for URFMP",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "echo \"No tests configured for @urfmp/types\" && exit 0",
    "prepublishOnly": "npm run build",
    "prepack": "npm run build"
  },
  "keywords": [
    "typescript",
    "types",
    "robotics",
    "urfmp"
  ],
  "author": "URFMP Team",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.3.0"
  },
  "files": [
    "dist",
    "src"
  ]
}
